package city.example.demo.repos;

import org.springframework.data.jpa.repository.JpaRepository;

import city.example.demo.models.AddTransport;

public interface AddTransportRepository extends JpaRepository<AddTransport,Long>{

}
